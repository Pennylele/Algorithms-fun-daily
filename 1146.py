# I was thinking whether the snap_id exist..
# The data structure could be a 2-D array [[self.snap_id, ]]
class SnapshotArray:
    def __init__(self):
        self.length = length
        self.snap = 0
        self.container = [[-1, -1, -1]] * self.length

    def set(self, index, val):

    
    def snap(self):

    def get(self, index, snap_id):
